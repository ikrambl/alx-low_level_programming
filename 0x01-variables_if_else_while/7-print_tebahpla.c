#include <stdio.h>
/**
 * main - C program that prints lc letters
 * Return: 0 (success)
 */
int main(void)
{
	int a = 122;

	for (a = 122; a >= 97; a--)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}

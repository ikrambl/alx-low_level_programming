#include <stdio.h>
/**
 * main - a C program prints single digit numbers
 * Return: 0 (success)
 */
int main(void)
{
	int a = 0;

	for (a = 0 ; a <= 9 ; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
